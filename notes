#include <iostream>
using namespace std;
int main() {
//  datatype + variable + value
    /*int myAge = 25;
    int votingAge = 18;
     
    cout << boolalpha << (myAge >= votingAge) << "\n"; // returns 1 (true), meaning 25 year olds are allowed to vote!*/
    
  /*  int myAge = 25;
    int votingAge = 18;

    if (myAge >= votingAge) {
    cout << "Old enough to vote!";
    } else {
    cout << "Not old enough to vote.";
    } */
    
    /*if (20 > 18) {
    cout << " 20 is greater than 18";
    }*/
    
    /*int x = 20;
    int y = 18;
    if (x > y) {
    cout << "x is greater than y";
    }*/
    
    /*int time = 20;
    if (time < 18) {
    cout << "Good day.";
    } else {
    cout << "Good evening.";
} */
    // Outputs "Good evening."
    // Outputs: Old enough to vote!
/*  int a = 100;
    int b = 60;
    if (a >= b) {
        cout << "im happy give me more";
    } else {
        cout << "im hungry give me money ";
    } */
